# AI 협업 방식 (AI Collaboration Workflow)

> 실무(CJ온스타일 운영)와 개인 연습에서 사용하는 AI 협업 프로세스를 정리했습니다.
> 원칙: **위임하기 전에 맥락을 완성하고, 결과물을 반드시 의심한다.**

---

## 1. 워크플로우 4단계

```
[1] 사전 분석          [2] 구조화 위임         [3] 범위 지정 수정      [4] 검증 후 반영
 API 사용 위치,    →   개요·참조 파일·     →   변경분만 지정해     →   영향도 / 데드코드
 기존 코드 파악        조건·명세 명문화        재생성 최소화           / 린트 통과 시 머지
```

- **사전 분석**: AI에게 요청하기 전, 관련 API의 사용 위치와 기존 코드 구조를 먼저 파악합니다. 내가 모르는 코드는 AI에게 정확히 시킬 수 없기 때문입니다.
- **구조화 위임**: 작업 개요 / 참조 마크업 / 영역별 노출 조건(폴백 포함) / API 명세의 4블록 프롬프트로 위임합니다.
- **범위 지정 수정**: 기획·API 변경 시 수정 위치를 지정해 위임합니다. 전체 재생성으로 인한 회귀와 토큰 낭비를 막습니다.
- **검증 후 반영**: 아래 체크리스트(3장)를 통과한 코드만 반영합니다.

---

## 2. 실제 위임 프롬프트 구조 (일반화 예시)

실무에서 신규 모듈(쿠폰 다운로드 공통 컴포넌트) 개발 시 사용한 초안 프롬프트의 구조입니다.
(API 주소·파라미터·기획 내용은 일반화했습니다)

```
1. 작업 개요
   모듈을 신설한다. 쿠폰을 다운로드하면 브랜드찜이 함께 처리되는 기능이다.

2. 마크업 참조
   [마크업 파일 경로 첨부] 이 파일의 구조를 따를 것.

3. 영역별 기능 및 노출 조건
   3-1. 소팅탭: 데이터가 유효할 때만 노출. 이미지가 없으면 기본이미지 노출(폴백)
   3-2. 쿠폰 영역: 기존 쿠폰 영역과 동일한 방식으로 노출.
        쿠폰 API는 미확정이므로 영역만 스텁으로 구현할 것
   3-3. 상품 영역: [상품 노출 조건 명세]
   3-4. 더보기 버튼: API 데이터가 있을 때만 노출. 문구·링크는 응답값 사용,
        문구가 없으면 기본 문구 '더보기' 노출(폴백)
        데이터: contentList.moreBtn.useYn (String, Y/N)

4. API 명세
   쿠폰 발행 API
   - Method: POST
   - Host: api.example.com
   - Path: /module/coupon (로그인 필수)
   - Params:
     - productCode: 상품코드 / Integer / 필수
     - couponCodes: 쿠폰코드 목록 / List / 필수 / 최대 10건(중복 가능)
```

**이 구조의 의도**

| 블록 | 막으려는 문제 |
|---|---|
| 참조 마크업 지정 | AI가 임의의 구조·스타일로 생성하는 것 |
| 폴백 규칙 명세 | 데이터 부재 시 AI가 임의 처리 → 운영 장애로 이어지는 것 |
| 미확정 API의 스텁 지시 | 스펙 확정을 기다리느라 일정이 밀리는 것 |
| 타입·필수 여부·최대 건수 명세 | 유효성 검증 누락, 잘못된 타입 처리 |

---

## 3. 검증 체크리스트

AI가 생성한 코드는 아래를 통과해야만 반영합니다.

**실무에서 사용하는 기본 3종**
- [ ] 기존 모듈 영향도: 기존 동작이 변하지 않았는가 (신규 로직은 분기로, 기존 로직은 기본 경로로 보존)
- [ ] 데드코드: 사용되지 않는 코드가 생성되지 않았는가
- [ ] 린트: 정적 검사 통과

**연습 프로젝트에서 확장한 항목**
- [ ] 엣지 케이스 강제 재현: 실데이터에 없는 상태(품절 등)를 임시 코드로 만들어 미검증 UI를 확인
- [ ] 실동작 검증: 디바운스 등은 코드가 아니라 Network 탭의 실제 호출로 확인
- [ ] 렌더링 경계 검사: 'use client' 사용 파일을 전수 확인, 상호작용 없는 컴포넌트의 클라이언트화 여부 점검
- [ ] SSR 확인: 초기 HTML에 데이터가 포함되는지 페이지 소스로 확인

---

## 4. 연습 프로젝트 — AI 협업 사이클 기록

> GitHub: [링크 추가]

- **Next.js(App Router) + TypeScript** 커머스 화면 연습 프로젝트
- AI에게 줄 프로젝트 룰(CLAUDE.md)을 백지에서 직접 설계: 폴더 구조, 서버/클라이언트 컴포넌트 기준,
  타입·에러 처리 컨벤션, "요구하지 않은 기능을 임의로 추가하지 말 것" 등의 출력 규칙 포함
- 위임 → 검증 → 수정 지시 사이클 기록:
  - 실데이터에 품절(stock 0) 상품이 없어 검증 불가 → 특정 상품의 재고를 강제로 0으로 만드는
    임시 코드를 지시해 품절 UI를 재현·확인 후 제거
  - 디바운스 요구(300ms) → Network 탭으로 실제 호출 횟수를 검증
- **AI 가설 반증 사례**: 올리브영 오늘드림 태그가 목록과 상세에서 다르게 보이는 현상에 대해
  AI가 제시한 '캐시 시간차' 가설을 서비스 재관찰로 반증 — 실제로는 옵션(색상) 단위의
  가능 여부를 목록에서 상품 단위로 집계(OR)해 표시하는 구조임을 확인

---

## 5. 도메인 분석 — 지원 전 서비스 역설계

지원 전, 올리브영 모바일 웹/앱을 프론트엔드 개발자 관점에서 분석했습니다.

- **밸류체인 지도**: 데이터 소스(사용자/상품/온라인 재고/오프라인 재고) ↔ 화면 흐름
  (홈 → 검색 → 상품상세 → 장바구니 → 주문)의 연결 관계를 도식화
  → [지도 이미지 첨부]
- **주요 관찰**
  - 오늘드림 가능 여부는 옵션 단위 속성이며, 목록은 이를 상품 단위로 집계해 태그 표시
  - 오프라인 재고는 오늘드림(상세·장바구니)과 매장 픽업(결제)이 서로 다른 시점에 다른 목적으로 조회
  - 비로그인 장바구니는 로그인 게이트로 병합 문제를 정책적으로 회피
  - 상품상세의 쿠폰받기 → 로그인 복귀 시 404 결함 발견, 새 창 컨텍스트로 인한
    히스토리 유실로 원인 추정
- 기술 스택 확인: 콘솔·네트워크 분석으로 React 기반 렌더링과 초기 HTML의 데이터 포함(SSR) 확인
